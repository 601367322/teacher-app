package com.prance.teacher.features.pk.rocket

import android.graphics.Bitmap
import android.graphics.PointF

class LittleRocket(point: PointF, collisionRadius: Float, override var bitmap: Bitmap) : BaseRocket(point,collisionRadius) {

}