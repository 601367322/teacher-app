package com.prance.teacher.features.pk.rocket

import android.graphics.Bitmap
import android.graphics.PointF

class BigRocket(point: PointF, collisionRadius: Float, override var bitmap: Bitmap) : BaseRocket(point, collisionRadius) {

}